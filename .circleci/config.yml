version: 2
jobs: 
  test: 
    docker: 
      - image: buildpack-deps:trusty
    steps:
      - run: 
    	    name: "say hi to circle"
    	    command: "echo hello circle"
workflows:
  version: 2
  runTest:
      jobs:
        - test