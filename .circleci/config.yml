version: 2
jobs: 
  donkey: 
    docker: 
      - image: buildpack-deps:trusty
    steps:
      - run: 
          name: "say hi to elephant"
          command: "echo hello elephant"
workflows:
  version: 2
  runTest:
    jobs:
        - donkey