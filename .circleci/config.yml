version: 2
jobs: 
  donkey: 
    docker: 
      - image: node:10.17.0-alpine
    steps:
      - checkout
      - run: 
          name: "install package manager"
          command: "npm install"
      - run:
          name: "run test"
          command: "jest"
workflows:
  version: 2
  runTest:
    jobs:
        - donkey